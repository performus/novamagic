/**
 * B10 — закрывающий тёмный блок (футер-панель). Заголовок раскалывается на две строки
 * по скроллу (line1 → влево, line2 → вправо), бутылка упирается в низ панели.
 */
export const closingContent = {
  line1: 'Дорогое звучание аромата',
  line2: 'для вещей, которые выбирают надолго',
  renderAlt: 'NOVAMAGIC Dubai Wind',
} as const
